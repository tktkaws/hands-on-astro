---
const { article } = Astro.props;
import Tag from "./Tag.astro";
console.log(article.tags);
---

<li class="c-card">
	<a href={`/articles/${article.slug}`}>
		<div class="image-container">
			<img src={`${article.coverImage.src}`} alt="" class="main-image" />
			<img src={`${article.coverImage.src}`} alt="" class="bg-image" />
		</div>
		<h2>
			{article.title}
		</h2>
		<!-- <ul class="c-card-tag-list">
			{
				article.tags &&
					article.tags.length > 0 &&
					article.tags.map((tag) => {
						return (
							<li>
								<a href={`/tags/${tag.slug}`}>
									<span>{tag.name}</span>
								</a>
							</li>
						);
					})
			}
		</ul> -->
	</a>
</li>
<style>
	.c-card {
		width: 300px;
		height: 360px;
		box-shadow: 0 0 6px 1px rgba(0, 0, 0, 0.16);
		background-color: #fff;
		/* border: solid 1px; */
		border-radius: 1rem;
	}
	.c-card a {
		width: 100%;
		height: 100%;
		display: block;
	}

	h2 {
		/* margin-top: 2rem; */
		/* padding: 1rem; */
		padding: 1rem;
		font-size: 1.25rem;
		font-weight: bold;
	}
	h1 {
		font-size: 4rem;
		border-bottom: solid 2px;
	}

	img {
		width: 100%;
		height: auto;
		aspect-ratio: 16/9;
		object-fit: contain;
		/* border-radius: 1rem; */
		border-top-left-radius: 1rem;
		border-top-right-radius: 1rem;
	}

	.image-container {
		position: relative;
		width: 100%;
		height: auto;
		aspect-ratio: 16/9;
		/* width: 300px; 
		height: 200px;  */
		overflow: hidden;
		border-radius: 1rem;
	}

	.main-image {
		width: 100%;
		height: 100%;
		object-fit: contain;
	}

	.bg-image {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		filter: blur(6px); /* ブラー効果の強さ */
		z-index: -1; /* 画像より背面に配置 */
	}
	/* * {
		border: solid 1px magenta;
	} */
</style>
